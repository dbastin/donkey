<?xml version="1.0" encoding="UTF-8"?>
<project name="base-resources" default="ignore">
    <import file="base-init.xml"/>

    <target name="resources">
        <copy todir="${prod.class.dir}">
            <fileset dir="${prod.src.dir}" includes="**/*.properties"/>
        </copy>
    </target>
</project>
