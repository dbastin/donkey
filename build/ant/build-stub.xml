<?xml version="1.0" encoding="UTF-8"?>
<project name="stub" default="ignore" basedir="../..">
    <import file="build-init.xml"/>

    <target name="-compile_stub">
        <macro_javac version="1.5"
                     srcdir="${stub.src.dir}"
                     destdir="${stub.class.dir}"
                     classpathref="stub.compile.classpath"/>

    </target>

    <target name="-stub" depends="-compile_stub">
        <jar jarfile="${runtime.lib.dir}/tibco/tibco.jar">
            <zipfileset dir="${stub.class.dir}"/>
        </jar>
    </target>
</project>